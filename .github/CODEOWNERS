* @bigezwest

# CODEOWNERS file 
# Lines starting with '#' are commnets.
# Each line is a file pattern followed by one or more owners. 

# The following example assumes 'bigezwest' shoud be the code woner ofr specific directories and file types. 

# To make 'bigezwest' the defualt owner for everything in the repo
# *     @bigezwest  

# To specify code ownership for specific directories
# /docs/      @bigezwest
# /src/       @bigezwest

# To specify code ownership for specific file types
# *.js      @bigezwest
# *.swift      @bigezwest

* To add more code owners, repeat the pattern bove with the directory, filepattern, or specific file, followed bny the owner.
# You cna also specify GitHub teams as woners using the @orgname/teamname syntax.

# Example for specifying a team (replace 'team-name' with the actual team neame) 
# /docs/       @bigezwest/team-name

# Remmeber to replace 'bigezwest' with th actual GigHub username and adjust the directories and file types according to your project's structure. 
